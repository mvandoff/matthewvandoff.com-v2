<div class="transition" id="transition"></div>

<style is:global>
	:root {
		/* --transition-bg: var(--main-950); */
		--transition-bg: black;
		--transition-block-size: var(--bg-block-size, 64px);
		/* --transition-bg: rgb(30, 30, 77); */
	}
	.transition {
		display: grid;
		/* Must sit above all page overlays (e.g. About `#block-grid` and `.tl-block` z-indexes). */
		z-index: 10000;
		background-color: var(--transition-bg);
		width: 100%;
		min-height: 100vh;
		position: fixed;
		inset: 0;
		grid-auto-columns: var(--transition-block-size);
		grid-auto-rows: var(--transition-block-size);
	}

	.transition-block {
		background-color: var(--transition-bg);
		width: 100%;
		height: 100%;
	}
</style>
<script>
	import { initTransition } from 'scripts/global/initTransition';

	initTransition();
</script>
